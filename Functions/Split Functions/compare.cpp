int max(int a, int b){
    if (a>b)
        return a;
    else    
        return b;
}
    // Defines functions here
int min(int a, int b){
    if (a < b)
        return a;
    else    
        return b;
}
int incr_mult(int a, int b){
    return ((++a) * (++b));
}
